name: Deploy to AWS ECS

on:
  push:
    branches:
      - main
  workflow_dispatch:  # 수동 실행 가능

env:
  AWS_REGION: ap-northeast-2
  ECR_REPOSITORY: sample-spring-app
  ECS_CLUSTER: test-ecs-cluster
  ECS_SERVICE: sample-task-service
  CONTAINER_NAME: sample-app

jobs:
  deploy:
    name: Build and Deploy to ECS
    runs-on: ubuntu-latest
    
    # OIDC 토큰 발급을 위한 권한 설정
    permissions:
      id-token: write
      contents: read
    
    steps:
      # 1. 코드 체크아웃
      - name: Checkout code
        uses: actions/checkout@v4

      # 2. JDK 11 설정
      - name: Set up JDK 11
        uses: actions/setup-java@v4
        with:
          java-version: '11'
          distribution: 'temurin'

      # 3. Gradle Wrapper JAR 직접 다운로드 (없는 경우)
      - name: Download Gradle Wrapper JAR
        run: |
          if [ ! -f gradle/wrapper/gradle-wrapper.jar ]; then
            echo "Downloading Gradle Wrapper JAR..."
            mkdir -p gradle/wrapper
            curl -sL https://github.com/gradle/gradle/raw/v7.6.1/gradle/wrapper/gradle-wrapper.jar -o gradle/wrapper/gradle-wrapper.jar
            
            # gradle-wrapper.properties 파일도 없으면 생성
            if [ ! -f gradle/wrapper/gradle-wrapper.properties ]; then
              echo "distributionUrl=https\://services.gradle.org/distributions/gradle-7.6.1-bin.zip" > gradle/wrapper/gradle-wrapper.properties
            fi
          fi

      # 4. Gradle 빌드 권한 부여
      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      # 5. Gradle 빌드 (bootJar)
      - name: Build with Gradle
        run: ./gradlew bootJar --no-daemon

      # 6. AWS 자격 증명 설정 (OIDC + Role 방식)
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      # 7. ECR 로그인
      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      # 8. Docker 이미지 빌드 및 푸시
      - name: Build, tag, and push image to Amazon ECR
        id: build-image
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }}
        run: |
          docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG .
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG
          docker tag $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG $ECR_REGISTRY/$ECR_REPOSITORY:latest
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:latest
          echo "image=$ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG" >> $GITHUB_OUTPUT

      # 9. ECS 태스크 정의에 새 이미지 ID 반영
      - name: Fill in the new image ID in the Amazon ECS task definition
        id: task-def
        uses: aws-actions/amazon-ecs-render-task-definition@v1
        with:
          task-definition: sample-task.json
          container-name: ${{ env.CONTAINER_NAME }}
          image: ${{ steps.build-image.outputs.image }}

      # 10. ECS 서비스에 새 태스크 정의 배포
      - name: Deploy Amazon ECS task definition
        uses: aws-actions/amazon-ecs-deploy-task-definition@v2
        with:
          task-definition: ${{ steps.task-def.outputs.task-definition }}
          service: ${{ env.ECS_SERVICE }}
          cluster: ${{ env.ECS_CLUSTER }}
          wait-for-service-stability: true

      # 11. 배포 완료 알림
      - name: Deployment completed
        run: |
          echo "🚀 Deployment completed successfully!"
          echo "Image: ${{ steps.build-image.outputs.image }}"
